spring:
  application:
    name: hustleup-subscription
  datasource:
    url: jdbc:mysql://localhost:3306/hustleup?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_PASSWORD:francis}
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8084

app:
  stripe:
    secret-key: ${STRIPE_SECRET_KEY:sk_test_51...} # Use environment variable
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:whsec_...}
  frontend:
    url: ${FRONTEND_URL:http://localhost:3000}
  jwt:
    secret: HustleUpSecretKeyForJWTTokenGenerationMustBe256BitsLongAtLeast!!2026
